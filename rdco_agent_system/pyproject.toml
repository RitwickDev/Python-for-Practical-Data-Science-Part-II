[project]
name = "rdco_agent_system"
version = "0.1.0"
description = "A multi-agent AI system for RD&Co Investment Banking."
authors = [
    { name = "Jules", email = "jules@example.com" },
]
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
